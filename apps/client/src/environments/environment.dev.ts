import { Environment } from './_environment';

export const environment: Environment = {
  production: false,
  apiUrl: 'local',
  authority: 'http://localhost:8080/realms/zenith-realm',
  clientId: 'zenith-client',
};
